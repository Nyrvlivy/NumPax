<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
            https://jakarta.ee/xml/ns/jakartaee
            https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- Configuração do Servlet do Jersey -->
    <servlet>
        <servlet-name>jersey-servlet</servlet-name>
        <servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>
        <init-param>
            <param-name>jakarta.ws.rs.Application</param-name>
            <param-value>br.com.numpax.infrastructure.config.ApplicationConfig</param-value>
        </init-param>
        <init-param>
            <param-name>jersey.config.server.provider.packages</param-name>
            <param-value>br.com.numpax.api.v1.controllers</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <!-- Mapeamento do Servlet do Jersey para URLs com prefixo /api/* -->
    <servlet-mapping>
        <servlet-name>jersey-servlet</servlet-name>
        <url-pattern>/api/*</url-pattern>
    </servlet-mapping>

    <!-- Filtro de Autenticação JWT -->
    <filter>
        <filter-name>JwtAuthenticationFilter</filter-name>
        <filter-class>br.com.numpax.infrastructure.config.auth.JwtAuthenticationFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>JwtAuthenticationFilter</filter-name>
        <url-pattern>/api/protected/*</url-pattern> <!-- Defina os endpoints que deseja proteger -->
    </filter-mapping>

    <!-- Configuração de Bem-Vindo -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

<!--    <error-page>-->
<!--        <error-code>404</error-code>-->
<!--        <location>404.jsp</location>-->
<!--    </error-page>-->
</web-app>
